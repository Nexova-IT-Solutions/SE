services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: giftboxlk
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: giftboxlk
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
